*** Begin Patch
*** Update File: app/profile/layout.tsx
@@
-import ProfileHeader from "@/components/headers/ProfileHeader"
-import { ProfileSidebar } from "@/components/sidebars/ProfileSidebar"
-import { SidebarProvider, SidebarInset, SidebarTrigger } from "@/components/ui/sidebar"
-import DynamicBreadcrumb from '@/components/navigation/DynamicBreadcrumb'
+import ProfileHeader from "@/components/headers/ProfileHeader"
+import { ProfileSidebar } from "@/components/sidebars/ProfileSidebar"
+import { SidebarProvider, SidebarInset, SidebarTrigger } from "@/components/ui/sidebar"
+import DynamicBreadcrumb from "@/components/navigation/DynamicBreadcrumb"
@@
 export default function ProfileLayout({ children }: ProfileLayoutProps) {
-  return (
-    <div className="min-h-screen bg-gray-50">
-      <ProfileHeader />
-      <DynamicBreadcrumb />
-      <SidebarProvider>
-        <div className="container mx-auto">
-          <div className="flex py-10 gap-10">
-            <ProfileSidebar />
-            <SidebarInset className="flex-1">
-              <div className="flex h-12 items-center gap-2 px-4 mb-6">
-                <SidebarTrigger />
-              </div>
-              <main id="main" role="main" tabIndex={-1}>
-                {children}
-              </main>
-            </SidebarInset>
-          </div>
-        </div>
-      </SidebarProvider>
-    </div>
-  );
+  return (
+    <div className="min-h-screen bg-gray-50">
+      <SidebarProvider>
+        {/* Sidebar fijo a la izquierda */}
+        <ProfileSidebar />
+
+        {/* Contenido principal alineado con el sidebar */}
+        <SidebarInset>
+          {/* Cabecera y breadcrumbs permanecen dentro del contenido para evitar desalineaciones */}
+          <ProfileHeader />
+          <div className="container mx-auto px-4 sm:px-6">
+            <DynamicBreadcrumb />
+
+            {/* Trigger para colapsar/expandir el sidebar */}
+            <div className="flex h-12 items-center gap-2 mb-4">
+              <SidebarTrigger />
+              <span className="text-sm text-muted-foreground hidden md:inline">Menú</span>
+            </div>
+
+            <main id="main" role="main" tabIndex={-1} className="pb-10">
+              {children}
+            </main>
+          </div>
+        </SidebarInset>
+      </SidebarProvider>
+    </div>
+  )
 }
*** End Patch